<% if @comment.valid? %>
  $('.js-comments').prepend("<%= escape_javascript(render(@comment)) %>");
  $('.new-comment').html("<%= pluralize(@comment.user.comments.count, 'comment') %>");
<% else %>
  $('.new-comment').html("<%= pluralize(@comment.user.comments.count, 'comment') %>");
<% end %>
